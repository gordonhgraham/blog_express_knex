<div class="container">
 {{#each users}}
 <div>
   <h4><a href="/users/{{this.id}}">{{this.first_name}} {{this.last_name}}</a></h4>
   <img class="circle" src="{{this.avatar}}" alt="{{this.first_name}} + {{this.last_name}}" />
   <p>{{this.bio}}</p>
 </div>
 {{/each}}
</div>
